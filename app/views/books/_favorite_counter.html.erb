          <% if book.favorited_by(current_user) %>
            <%= link_to book.favorites.count, book_favorites_path(book.id), method: :delete, class: "glyphicon glyphicon-heart red" %>
          <% else %>
            <%= link_to book.favorites.count, book_favorites_path(book.id), method: :post, class: "glyphicon glyphicon-heart white" %>
          <% end %>